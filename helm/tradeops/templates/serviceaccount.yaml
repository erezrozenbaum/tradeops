{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tradeops.serviceAccountName" . }}
  labels:
    {{- include "tradeops.labels" . | nindent 4 }}
{{- end }}
